### Prototype agent
- `evaluator`: 
  - write tool to persist evaluation in ToolsContext
  - connect to interviewer as agent-as-a-tool

### Build webapp
- IIUC the current front-end is more like a debugging tool to test the agent
- build the *true* UI for better UX

